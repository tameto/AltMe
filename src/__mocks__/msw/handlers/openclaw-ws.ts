// OpenClaw WebSocket handler placeholder
// MSW v2 WebSocket support is still evolving.
// For now, this exports an empty array.
// When testing WebSocket connections, use direct mocking via jest.mock().

export const openclawWsHandlers: never[] = [];
